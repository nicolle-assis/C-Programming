#include <stdio.h>

int main() {
    int x = 10, y = 5;
    int soma = x + y;
    int diferenca = x - y;

    printf("x = %d\n", x);
    printf("y = %d\n", y);
    printf("Soma: %d\n", soma);
    printf("Diferença: %d\n", diferenca);

    return 0;
}
