#include <stdio.h>
#include <stdlib.h>

int main() {

    int x, y;
    system("cls");

    // Atribuição de valores às variáveis
    x = 10;  // x recebe 10
    y = 20;  // y recebe 20


    printf("%i", x); // Mostra o valor de x
    printf("\n%i", y); // Mostra o valor de y em uma nova linha

    return 0;
}