#include <stdio.h>
#include <stdlib.h>

int main()
{   
    system("cls");

    int num;

    printf("Informe um valor numerico valido: ");
    scanf("%d", num);

    
        if(num<=0)
            printf("error!valor invalido, insira uma novo valor");
      

    return 0;
}